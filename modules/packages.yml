# yaml-language-server: $schema=https://schema.blue-build.org/module-v1.json
type: rpm-ostree
install:
  - firefox
  - gedit
  - gparted
  - kid3
  - libreoffice
  - pluma
  - rhythmbox
  - samba
  - samba-client
  - simple-scan
  - syncthing
  - vlc
  - yt-dlp
  
  ## Snaps run sudo snap install snap-store and sudo mkdir /var/lib/snapd/void before installing snaps to avoid error after os installation
  # snapd 
  
  ## Virt-Manager
  # libvirt-daemon-kvm
  - qemu-kvm
  # virt-install
  # virt-manager
  # virt-viewer
