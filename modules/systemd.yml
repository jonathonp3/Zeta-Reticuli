- type: systemd
  system: 
    enabled:
      # libvirtd.service
      - nfs-server.service
      - rpcbind.service
      - smb.service
      # snapd.service
      - sshd.service
  
  user: {} # Also add Flathub user repo, but no user packages
